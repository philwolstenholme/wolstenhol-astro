// Keyboard shortcuts for the Spotify cards, in on-screen order: card n is
// played with the digit key n+1 (with 0 as the tenth). Shared between the
// server-rendered key hints and the client-side key handler so they can't
// drift apart.
export const DIGIT_KEYS = ["1", "2", "3", "4", "5", "6", "7", "8", "9", "0"];
