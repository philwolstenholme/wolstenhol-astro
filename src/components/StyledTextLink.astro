---
interface Props {
  href: string;
  class?: string;
  [key: string]: unknown;
}
const { href, class: className = "", ...rest } = Astro.props;
---

<a href={href} class:list={["underline", className]} {...rest}><slot /></a>
