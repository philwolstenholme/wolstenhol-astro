---
import { paginate } from "../helpers/paginate";
import Pagination from "./Pagination.astro";

const {
  items,
  itemsPerPage,
  searchParam = "page",
  anchor,
  onlyFullPages = true,
  itemComponent: Item,
  htmxPartialPath,
  pageUrl,
  staticMode = false,
  prevUrl,
  nextUrl,
} = Astro.props as {
  items: readonly unknown[];
  itemsPerPage: number;
  searchParam?: string;
  anchor?: string;
  onlyFullPages?: boolean;
  itemComponent: any; // Component expected to output its own <li>
  htmxPartialPath?: string;
  pageUrl?: URL;
  staticMode?: boolean;
  prevUrl?: string | null;
  nextUrl?: string | null;
};

const { data, hasPrev, hasNext, prevIndex, nextIndex } = staticMode
  ? {
      data: items as unknown[],
      hasPrev: prevUrl != null,
      hasNext: nextUrl != null,
      prevIndex: null as null,
      nextIndex: null as null,
    }
  : paginate({
      onlyFullPages,
      items,
      itemsPerPage,
      url: new URL(Astro.request.url),
      searchParam,
    });

const htmxAttrs = staticMode
  ? {}
  : {
      "x-data": "",
      "@htmx:after-swap": `$event.target.id === 'paginated-grid-${searchParam}' && $focus.within($event.target).first()`,
    };
---

{
  data && data.length > 0 ? (
    <div {...htmxAttrs}>
      <div id={`paginated-grid-${searchParam}`}>
        <ul class="grid gap-4 grid-cols-[repeat(auto-fit,minmax(330px,1fr))]">
          {data.map((item: any) => (
            <Item key={item.id} item={item} />
          ))}
        </ul>
        <Pagination
          anchor={anchor}
          hasPrev={hasPrev}
          hasNext={hasNext}
          prevIndex={prevIndex}
          nextIndex={nextIndex}
          searchParam={searchParam}
          htmxPartialPath={htmxPartialPath}
          pageUrl={pageUrl}
          itemsPerPage={itemsPerPage}
          prevUrl={prevUrl}
          nextUrl={nextUrl}
        />
      </div>
    </div>
  ) : (
    <slot name="empty" />
  )
}
