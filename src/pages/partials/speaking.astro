---
export const prerender = false;

import SpeakingCard from "../../components/items/SpeakingCard.astro";
import PaginatedGrid from "../../components/PaginatedGrid.astro";
import { getSortedSpeaking } from "../../helpers/getSpeakingSorted";
import { preparePartialPage } from "../../helpers/preparePartialPage";

const searchParam = "speaking";
const htmxPartialPath = "/partials/speaking";

const items = await getSortedSpeaking();

const { itemsPerPage, pageUrl } = preparePartialPage(Astro, {
  items,
  searchParam,
});
---

<PaginatedGrid
  items={items}
  itemsPerPage={itemsPerPage}
  searchParam={searchParam}
  itemComponent={SpeakingCard}
  htmxPartialPath={htmxPartialPath}
  pageUrl={pageUrl}
/>
